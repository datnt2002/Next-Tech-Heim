{
  "extends": ["@commitlint/config-conventional"],
  "formatter": "@commitlint/format",
  "rules": {
    "type-enum": [2, "always", ["fix", "feat", "chore", "test", "build"]],
    "type-empty": [2, "never"],
    "subject-empty": [2, "never"]
  }
}
